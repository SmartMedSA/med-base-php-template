name: Waypoint

on:
  workflow_call:
    inputs:
      workspace:
        required: true
        type: string
      tag:
        required: true
        type: string
      ingress:
        required: false
        type: string
        default: '{class="nginx",target="medv2-stage-tn-ec1-ingress.med.ovh", zone="med.ovh"}'
    secrets:
      token:
        required: true
      encodedString:
        required: true
      WAYPOINT_SERVER_TOKEN:
        required: true
      WAYPOINT_SERVER_ADDR:
        required: true
      WAYPOINT_SERVER_TLS:
        required: false
      WAYPOINT_SERVER_TLS_SKIP_VERIFY:
        required: false

env:
  WAYPOINT_SERVER_TOKEN: ${{ secrets.WAYPOINT_SERVER_TOKEN }}
  WAYPOINT_SERVER_ADDR: ${{ secrets.WAYPOINT_SERVER_ADDR }}
  WAYPOINT_SERVER_TLS: ${{ secrets.WAYPOINT_SERVER_TLS }}
  WAYPOINT_SERVER_TLS_SKIP_VERIFY: ${{ secrets.WAYPOINT_SERVER_TLS_SKIP_VERIFY }}

jobs:
  waypoint_up:
    name: waypoint up
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: System - install dependencies
        run: |
          sudo apt-get install apt-transport-https --yes

          wget -O- https://apt.releases.hashicorp.com/gpg | gpg --dearmor | sudo dd status=none of=/usr/share/keyrings/hashicorp-archive-keyring.gpg
          echo "deb [signed-by=/usr/share/keyrings/hashicorp-archive-keyring.gpg] https://apt.releases.hashicorp.com $(lsb_release -cs) main" | sudo tee /etc/apt/sources.list.d/hashicorp.list

          curl https://baltocdn.com/helm/signing.asc | gpg --dearmor | sudo tee /usr/share/keyrings/helm.gpg > /dev/null
          echo "deb [arch=$(dpkg --print-architecture) signed-by=/usr/share/keyrings/helm.gpg] https://baltocdn.com/helm/stable/debian/ all main" | sudo tee /etc/apt/sources.list.d/helm-stable-debian.list

          sudo apt update && sudo apt install waypoint helm

      - name: Docker - login container registry
        uses: docker/login-action@v2
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.token }}

      - name: Checkout
        uses: actions/checkout@v3

      - name: System - setup KUBECONFIG
        id: kubeconfig
        uses: timheuer/base64-to-file@v1.1
        with:
          fileName: 'config'
          encodedString: ${{ secrets.encodedString }}

      - name: Helm - update dependencies
        run: |
          helm dependency update chart/

      - name: Waypoint - up
        env:
          KUBECONFIG: ${{ steps.kubeconfig.outputs.filePath }}
          WP_VAR_registry_auth_username: ${{ github.actor }}
          WP_VAR_registry_auth_password: ${{ secrets.token }}
          WP_VAR_registry_image_tag: ${{ inputs.tag }}
        run: |
          waypoint init -plain
          waypoint up -plain \
            -w ${{ inputs.workspace }} \
            -var='ingress=${{ inputs.ingress }}'
